{
  "name": "lazyportfolio-tree-studio",
  "private": true,
  "version": "0.0.0",
  "description": "Behavioral test harness for project/tree_studio.html (Node's built-in test runner + jsdom).",
  "type": "module",
  "scripts": {
    "test": "node --test tests/js/tree_studio_v2_contract.test.mjs"
  },
  "devDependencies": {
    "jsdom": "^24.0.0"
  }
}
